/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://www.buyget.co', // Replace with your actual domain
    generateRobotsTxt: true, // (optional) Generate a robots.txt file
    // optional: additional options
    // exclude: ['/secret-page'], // Exclude specific routes
    // sitemapSize: 7000, // Split sitemap if you have many URLs
  };
  